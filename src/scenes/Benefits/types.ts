import { SelectedPage } from "../../shared/types";

export type BenefitsPropsTypes = {
  selectPageHandler: (value: SelectedPage) => void;
};

export type GraphicsPropsTypes = {
  selectPage: () => void;
};
